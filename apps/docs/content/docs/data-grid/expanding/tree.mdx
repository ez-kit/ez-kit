---
title: Expanding · Tree
description: Render hierarchical data as expandable parent/child rows that share the column set.
---

When your data is hierarchical (folders, org charts, BOMs), render it as a tree. Parent and child rows share the column set.

## Usage

<DataGridDocsExample exampleId='expanding-tree' />

```tsx
<DataGrid
	data={data} // each row has optional `children: TRow[]`
	columns={columns}
	expanding={{ mode: 'tree' }}
/>
```

## Custom sub-rows key

`getSubRows` tells the grid where the children live when they are not on `children`.

<DataGridDocsExample exampleId='expanding-tree-sub-rows' />

```tsx
<DataGrid
	data={data} // each row has optional `reports: TRow[]`
	columns={columns}
	expanding={{
		mode: 'tree',
		getSubRows: (row) => row.reports,
	}}
/>
```

## Options

| Option                 | Type                                  | Description                                                  |
| ---------------------- | ------------------------------------- | ------------------------------------------------------------ |
| `expanding.mode`       | `'sub-content' \| 'tree'`             | Mode switch. Default `'sub-content'`.                        |
| `expanding.getSubRows` | `(row, index) => TRow[] \| undefined` | Sub-row extractor. Auto-detects `row.children` when omitted. |

## Source

- `@ez-kit/data-grid-react` — [`packages/data-grid/react/react/src/use-data-grid.ts`](https://github.com/ez-kit/ez-kit/blob/main/packages/data-grid/react/react/src/use-data-grid.ts)
- `@ez-kit/data-grid-core` — [`packages/data-grid/core/src/types.ts`](https://github.com/ez-kit/ez-kit/blob/main/packages/data-grid/core/src/types.ts)
