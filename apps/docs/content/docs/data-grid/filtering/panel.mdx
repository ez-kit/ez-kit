---
title: Filter Panel
description: Render a side-panel filter UI with all filterable columns visible at once, plus chip-based active filter display.
---

# Filter Panel

When per-column popovers feel cramped, switch to a side panel that lists every filterable column in one place.

## Usage

The panel variant collects every column filter into a single row of chips instead of the header. Each chip shows `label: value` (or `Any` when empty); clicking it opens a popover with the input, and the X clears it. The header drops its inline filters but stays sortable.

<DataGridDocsExample exampleId='filter-panel' />

The panel is a `<DataGrid.FilterPanel />` slot. Each row corresponds to a filterable column; the engine reads `meta.filterVariant` and `meta.operators` to pick controls.

A complementary surface is the active-filters chip strip. With `filtering: { chips: true, clearButton: true }`, a chip appears per active filter and the clear button auto-mounts once at least one filter is set.

<DataGridDocsExample exampleId='filter-chips-auto' />

Pass `clearButton: { alwaysShow: true }` to keep the clear button mounted even when no filter is active.

<DataGridDocsExample exampleId='filter-chips-always' />

For full control, compose the toolbar yourself — drop `<DataGrid.ActiveFiltersBar />` and `<DataGrid.ClearFiltersButton />` wherever the layout calls for them.

<DataGridDocsExample exampleId='filter-chips-custom' />

```tsx
<DataGrid.ActiveFiltersBar />
<DataGrid.ClearFiltersButton />
```

## Options

| Option | Type | Description |
| --- | --- | --- |
| `meta.filterVariant` | `'text' \| 'number' \| 'select' \| 'multiselect' \| 'date' \| 'dateRange'` | Drives the input rendered in the panel. |
| `meta.options` | `Array<{ label, value }>` | Required when variant is `'select'` or `'multiselect'`. |
| `meta.operators` | `Operator[]` | See [Operators](./data-grid/filtering/operators). |

## See also

- [Operators](./data-grid/filtering/operators)
- [Date range](./data-grid/filtering/date-range)
- [Multi-value](./data-grid/filtering/multi-value)

## Source

- `@ez-kit/data-grid-react` — [`packages/data-grid/react/react/src/data-grid/filter-panel.tsx`](https://github.com/easylimeyep/ez-kit/blob/main/packages/data-grid/react/react/src/data-grid/filter-panel.tsx)
