# Changesets

Use Changesets to track package-level changes for `@ez-kit/ui` and `@ez-kit/styling`.

## Create a changeset

```bash
pnpm changeset
```

## Version packages

```bash
pnpm version-packages
```

## Publish

```bash
pnpm release
```
