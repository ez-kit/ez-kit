---
'@ez-kit/data-grid-heroui': minor
---

Drop the word "selected" from the floating selection bar — the count now reads as a bare badge (a HeroUI `Chip` in the heroui kit) followed by the separator and the actions, with the full label moved to `aria-label` for screen readers.
