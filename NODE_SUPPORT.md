# Node.js support policy

`ez-kit` packages support active Node.js LTS versions only.

Policy:

- New features target current active LTS.
- Security and bug fixes are validated on active LTS lines.
- End-of-life Node versions are not tested in CI and are considered unsupported.
