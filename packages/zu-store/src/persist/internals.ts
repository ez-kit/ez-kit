/**
 * Engine internals, re-exported from `@ez-kit/store-persist/internals`. Import these ONLY when
 * authoring a custom source adapter or extending the engine. A binding built by hand from these
 * takes this package's `zustandPort` (exported from `@ez-kit/zu-store/persist`) as its port.
 */
export * from '@ez-kit/store-persist/internals'
