/** The `type` attribute a text input may carry — a closed set, never a bare string. */
export enum TextInputType {
	Text = 'text',
	Email = 'email',
	Password = 'password',
	Tel = 'tel',
	Url = 'url',
	Search = 'search',
}
